##Key Concepts
Local Scope: Variables defined inside a function are local to that function and cannot be directly accessed from outside it.
Return Statement: This is how a function sends a result back to where it was called.
Function Parameters: These are variables defined in the function signature that receive values (arguments) when the function is called.